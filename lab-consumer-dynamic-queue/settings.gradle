pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}
rootProject.name = 'lab-consumer-dynamic-queue'

include ':lab-common'

project(":lab-common").projectDir = new File(settingsDir, "../lab-common")